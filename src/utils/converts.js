export const convertDataTrigger = (incomes, expences) => {
    const convertedData = [];

    const exampleData = {
        name: 'date',
        income: 3000,
        expence: 4000,
    };

    return convertedData;
};
